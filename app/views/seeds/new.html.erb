
<section id="content">
	<div class="container">
            <div class="col-md-12">
                <div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Add yourself</h3>
  </div>
  <div class="panel-body">
      
      <div class="row">  
          <%= form_for :person, url: {action: "create"}, html: {class: "form-horizontal"} do |p| %>
       <div class="form-group">
        <label for="select" class="control-label">Choose Father</label>
          <div class="col-lg-4">
          <%= p.collection_select(:father, @persons,:id, :name, {prompt: "Select category"}, {class: "form-control"})  %>
                         
          </div>
       </div>
  
      <div class="form-group">
      <label for="select" class="control-label">Choose Son</label>
      <div class="col-lg-4">
          <%= p.collection_select(:seeds, @persons,:id, :name, {prompt: "Select category"}, {class: "form-control"})  %>
                         
      </div>
    </div>
          
          <%= p.submit "Adopt this Person", class: "btn btn-primary" %>
      </div>
      <% end %>
      </div>
</div>
            </div>
            
            <!---Start Orphans show--->
            <div class="row">
                <% @persons.each do |t| %>
                           
           <%= link_to(t) do%>  <span class="label label-info"><%= t.name %> </span> 
           <% end %>
                       
                <% end %>
                                               
            </div>
            <!---End of Tribes show--> 
        	<!-- divider -->
                
		<div class="row">
			<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>#</th>
      <th>Column heading</th>
      <th>Column heading</th>
      <th>Column heading</th>
    </tr>
  </thead>
  <tbody>
      <% @persons.seeds.each_rel do |r| %>
    <tr>
      <td>1</td>
      <td><%= r.from_node[:name] %></td>
      <td><%= r.to_node[:name] %> </td>
      <td>Column content</td>
    </tr>
    <% end %>
    </tbody>
</table> 
		</div>
		<!-- end divider -->
				
	</div>
	</section>
